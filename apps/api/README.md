# `@devibe/api`

Stub for NestJS (or Cloudflare Workers) API services.

- Prefer edge-first handlers; heavier NestJS services can promote later via adapters
- Database: start with D1; Postgres/pgvector when needed (`packages/database`)
